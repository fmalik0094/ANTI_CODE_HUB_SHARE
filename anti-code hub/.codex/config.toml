# Verified against the official Codex configuration reference on 2026-09-10:
# https://learn.chatgpt.com/docs/config-file/config-reference
# Migration: https://learn.chatgpt.com/docs/agent-approvals-security
#
# Approval behavior is inherited from app/user/managed policy. Deliberately omit
# approval_policy; the retired "untrusted" value prevents current runtime loading.
# This seed does NOT guarantee review of every command or a command-name deny list.
# Project trust and higher-priority configuration determine whether these defaults
# apply. See .agents/resources/CODEX_COMPATIBILITY.md before changing this contract.

sandbox_mode = "workspace-write"

[sandbox_workspace_write]
network_access = false
