# WORKFLOW: 02_ENTRY_AND_PROPOSE

Run at the start of every working session.

1. Read `.agents/ORIENTATION.md` — single entry point, settled decisions.
2. Claim exactly one identity from `.agents/AGENT_REGISTRY.md`. State it.
3. Confirm the active git branch and working-tree status. Create your work
   branch (`gem|cdx|cld/<item>`) if you don't have one — never work on `main`.
4. Read `.state/DECISIONS.md` for accepted architectural decisions.
5. Formulate an implementation proposal naming exact write paths.
6. Halt for `[PENDING OPERATOR APPROVAL]` before any mutation.
