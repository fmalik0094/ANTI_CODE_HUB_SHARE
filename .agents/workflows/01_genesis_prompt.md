# WORKFLOW: 01_GENESIS_PROMPT
1. Read `.agents/rules/global.md` and claim active role.
2. Initialize local state from `.state/BASELINE.md`.
3. Verify layout topology by running `python validators/validate_structure.py`.
4. Hold for user approval before mutating code.
